spring:
  application:
    name: auth-service

  # Configuration Base de données MongoDB
  data:
    mongodb:
      uri: mongodb://localhost:27017/auth_db?authSource=admin
      database: auth_db

  # Configuration Email
  mail:
    host: smtp.gmail.com
    port: 587
    username: zariateya@gmail.com # Remplacez par votre email
    password: izsnmmlcjpcfdqcj # Mot de passe d'application
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

# Configuration Serveur
server:
  port: 8081
  servlet:
    context-path: /api

# Configuration JWT
jwt:
  secret: "dGhpc2lzYWpheWF0c2VjcmV0Zm9yaGp3dGhlcmVpc2Fsb25nc3RyaW5nZm9yaG1hY3NoYTI1NmtleQ"
  expiration: 86400000 # 24 heures en millisecondes
  refresh-expiration: 604800000 # 7 jours en millisecondes

# Configuration Application
app:
  email:
    from-name: "Université SOA"

# Configuration Logging
logging:
  level:
    com.university.auth_service: DEBUG
    org.springframework.security: DEBUG
    org.springframework.mail: DEBUG
